<%= form_for([@user, @trail]) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :image_path %>
  <%= f.text_field :image_path %>

  <%= f.label :tag %>
  <%= f.collection_select(:tag_id, Tag.all, :id, :name) %>

  <%= f.submit %>


<% end %>
